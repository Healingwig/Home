# Núcleo de la API y la web
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.4
itsdangerous>=2.2.0
python-multipart>=0.0.9
pydantic>=2.8.0
httpx>=0.27.0

# Descarga del vídeo de Instagram
yt-dlp>=2024.12.13

# Almacenamiento de las recetas en Google Cloud Storage (STORAGE_BACKEND=gcs).
# Con STORAGE_BACKEND=local no se usa, pero pesa poco y evita sorpresas.
google-cloud-storage>=2.18.0

# Los backends de modelo hablan por HTTP: Gemini y Ollama no necesitan nada
# más. Extras opcionales:
#   requirements-whisper.txt   transcribir el audio en local (no hace falta
#                              con Gemini, que ya escucha el vídeo)
#   requirements-anthropic.txt usar Claude (de pago)
